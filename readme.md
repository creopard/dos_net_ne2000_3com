The project has been moved to https://gitlab.com/creopard/dos_net_ne2000_3com.

This is due to GitHub's limitations in storing files with CRLF line endings correctly, which is crucial for the NET.CFG file.

Downloading the ZIP file from here converts the line endings from CRLF to LF, meaning that the LSL.COM cannot read the NET.CFG file properly.